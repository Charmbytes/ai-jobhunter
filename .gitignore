# Your personal config holds API keys + contact info — never commit it
config.yaml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.Python

# Local app state (also lives in ~/.jobhunter, but ignore if copied in)
.jobhunter/
state.json

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
